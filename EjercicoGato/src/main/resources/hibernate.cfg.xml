<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Conexión a la base de datos -->
        <!-- DRIVER Y DATOS DE CONEXION DE LA BASE DE DATOS CORRESPONDIENTE-->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/uax</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">1234</property>

        <!-- Dialecto MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- No mostrar SQL ni logs -->
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">false</property>

        <!-- Actualización automática -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping -->
        <!-- AQUI SE MAPEAN LAS CLASES CON LAS QUE VAMOS A TRABAJAR EN LA BASE DE DATOS -->
        <mapping class="uax.model.Gato"/>
    </session-factory>
</hibernate-configuration>